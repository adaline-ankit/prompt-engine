# Python SDK

The primary Python SDK is the root `prompt_engine` package.

Install:

```bash
pip install -e .
```

See [sdk/python/examples/basic.py](examples/basic.py) for a minimal integration example.
